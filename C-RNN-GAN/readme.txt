1. set up the data.npy file by the utils.py, should be normalized first (we have functions for the three different datasets)
2. train the model by the jupyter notebook
3. use the c-rnn-gan file model.sample function to generate new data samples; (In the later there is an example). 
